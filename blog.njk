---
layout: layout.njk
title: Blog da SoftWave
---

<h1>{{ title }}</h1>

<ul>
  {% for post in collections.posts reversed %}
    <li style="margin-bottom: 2rem;">
      <a href="{{ post.url }}">
        <h2>{{ post.data.title }}</h2>
      </a>
      <small>{{ post.date | date("dd/MM/yyyy") }}</small><br>
      {% if post.data.thumbnail %}
        <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}" style="max-width: 300px;">
      {% endif %}
      <p><a href="{{ post.url }}">Leia mais →</a></p>
    </li>
  {% endfor %}
</ul>
