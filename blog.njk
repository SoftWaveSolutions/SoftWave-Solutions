---
layout: layout.njk
title: Soft Blog
permalink: "blog/index.html"
---

  <h1 class="blog-title">Bem-vindo ao Soft Blog</h1>
  <p class="blog-text">Seu portal de notícias tecnológicas</p>

  <section class="blog-grid">
    {% for post in collections.posts | reverse %}
      <div class="post">
        <img src="{{ post.data.thumbnail }}" alt="Imagem do post">
        <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
        <p>{{ post.templateContent | safe | striptags | truncate(100, true, '...') }}</p>
        <a href="{{ post.url }}">Ler mais</a>
      </div>
    {% endfor %}
  </section>
